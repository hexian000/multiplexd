{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "multiplexd configuration",
    "description": "Configuration file for multiplexd. At least one of 'mux_listen', 'mux_connect', or 'identity.mux_connect' must be present.",
    "type": "object",
    "properties": {
        "type": {
            "description": "MIME type identifying the config format and version.",
            "type": "string",
            "const": "application/x-multiplexd-config; version=1"
        },
        "api_listen": {
            "description": "Listen address for the HTTP management API (e.g. '127.0.0.1:9081').",
            "type": "string"
        },
        "mux_listen": {
            "description": "Address to accept inbound mux connections on (server mode).",
            "type": "string"
        },
        "mux_connect": {
            "description": "Address to dial for the outbound mux connection (client mode).",
            "type": "string"
        },
        "listen": {
            "description": "Local TCP address to accept application traffic on.",
            "type": "string"
        },
        "connect": {
            "description": "Forwarding target address for inbound application streams.",
            "type": "string"
        },
        "loglevel": {
            "description": "Log verbosity: 0=silence, 1=fatal, 2=error, 3=warning, 4=notice, 5=info, 6=debug, 7=verbose, 8=veryverbose. Default: 4.",
            "type": "integer",
            "minimum": 0,
            "maximum": 8,
            "default": 4
        },
        "max_sessions": {
            "description": "Maximum number of concurrent mux sessions. 0 means unlimited.",
            "type": "integer",
            "minimum": 0
        },
        "max_startups": {
            "description": "Unauthenticated connection throttle in 'start:rate:full' format. Beyond 'start' connections, each new one is dropped with probability 'rate'% until the queue reaches 'full'.",
            "type": "string",
            "pattern": "^[0-9]+:[0-9]+:[0-9]+$",
            "examples": [
                "10:30:60"
            ]
        },
        "tls": {
            "description": "TLS configuration. All three of cert, key, and authcerts must be set together, or all must be absent (plaintext mode).",
            "type": "object",
            "properties": {
                "cert": {
                    "description": "PEM certificate. Use '@path' to read the file at startup, or provide an inline PEM string.",
                    "type": "string"
                },
                "key": {
                    "description": "PEM private key. Use '@path' to read the file at startup, or provide an inline PEM string.",
                    "type": "string"
                },
                "ciphersuites": {
                    "description": "Colon-separated list of TLS 1.3 ciphersuites to enable.",
                    "type": "string"
                },
                "authcerts": {
                    "description": "Authorized peer certificates. Each entry is a '@path' reference or an inline PEM string.",
                    "type": "array",
                    "items": {
                        "type": "string"
                    },
                    "minItems": 1
                }
            },
            "additionalProperties": false
        },
        "mux": {
            "description": "Mux protocol and socket settings for the transport-level connection.",
            "type": "object",
            "properties": {
                "nodelay": {
                    "description": "Disable the mux-level Nagle algorithm: send frames immediately without waiting to coalesce with unacknowledged data. Default: true.",
                    "type": "boolean",
                    "default": true
                },
                "tcp": {
                    "description": "TCP socket options for the mux (transport-level) connection.",
                    "type": "object",
                    "properties": {
                        "reuseport": {
                            "description": "Enable SO_REUSEPORT on the mux socket. Default: false.",
                            "type": "boolean",
                            "default": false
                        },
                        "keepalive": {
                            "description": "Enable TCP keepalive on the mux socket. Default: false.",
                            "type": "boolean",
                            "default": false
                        },
                        "nodelay": {
                            "description": "Enable TCP_NODELAY on the mux socket. Default: true.",
                            "type": "boolean",
                            "default": true
                        },
                        "sndbuf": {
                            "description": "SO_SNDBUF hint in bytes for the mux socket. 0 uses the system default.",
                            "type": "integer",
                            "minimum": 0
                        },
                        "rcvbuf": {
                            "description": "SO_RCVBUF hint in bytes for the mux socket. 0 uses the system default.",
                            "type": "integer",
                            "minimum": 0
                        },
                        "notsent_lowat": {
                            "description": "TCP_NOTSENT_LOWAT threshold in bytes for the mux socket. 0 leaves the OS default unchanged. Default: 131072.",
                            "type": "integer",
                            "minimum": 0,
                            "default": 131072
                        },
                        "backlog": {
                            "description": "Listen backlog for the mux socket. Clamped to [1, 4096]. Default: 16.",
                            "type": "integer",
                            "minimum": 1,
                            "maximum": 4096,
                            "default": 16
                        }
                    },
                    "additionalProperties": false
                },
                "max_halfopen": {
                    "description": "Maximum half-open (unacknowledged SYN) streams per session. 0 disables the limit; otherwise clamped to [1, 4096]. Default: 256.",
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 4096,
                    "default": 256
                },
                "max_streams": {
                    "description": "Maximum concurrent streams per session. 0 means unlimited.",
                    "type": "integer",
                    "minimum": 0
                },
                "connect_timeout": {
                    "description": "Maximum time in seconds for a single TCP-connect and mux-handshake attempt. Clamped to [5, mux.timeout]. Default: 15.",
                    "type": "integer",
                    "minimum": 5,
                    "maximum": 86400,
                    "default": 15
                },
                "timeout": {
                    "description": "Session inactivity timeout in seconds. Clamped to [5, 86400]. Default: 60.",
                    "type": "integer",
                    "minimum": 5,
                    "maximum": 86400,
                    "default": 60
                },
                "keepalive": {
                    "description": "Keepalive probe interval in seconds. Clamped to [1, mux.timeout]. Default: 25.",
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 86400,
                    "default": 25
                },
                "send_timeout": {
                    "description": "Send completion timeout in seconds. Clamped to [5, mux.timeout]. Default: 15.",
                    "type": "integer",
                    "minimum": 5,
                    "maximum": 86400,
                    "default": 15
                },
                "idle_timeout": {
                    "description": "Session idle timeout in seconds. 0 disables idle eviction; otherwise clamped to [5, 31557600]. Default: 0.",
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 31557600,
                    "default": 0
                },
                "resume_timeout": {
                    "description": "Maximum time in seconds a suspended server session waits for a client reconnect. Must be >= mux.timeout; values below mux.timeout are clamped up to mux.timeout. Default: 120.",
                    "type": "integer",
                    "minimum": 5,
                    "maximum": 86400,
                    "default": 120
                },
                "session_window": {
                    "description": "Maximum total in-flight frames per session. Both stream_window and session_window must be 0 for automatic BDP-driven sizing; a mixed configuration (exactly one set to 0) produces a warning and treats both as 0. Fixed range when non-zero: [65536, 268435456]. Default: 0.",
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 268435456,
                    "default": 0
                },
                "stream_window": {
                    "description": "Per-stream receive buffer size in bytes. Both stream_window and session_window must be 0 for automatic BDP-driven sizing (starts at 65536, grows as bandwidth samples arrive); setting only one to 0 produces a warning and enables automatic mode for both. Fixed range when non-zero: [16384, 16777216]. Default: 0.",
                    "type": "integer",
                    "minimum": 0,
                    "maximum": 16777216,
                    "default": 0
                },
                "mem_pressure": {
                    "description": "Receive-buffer memory pressure thresholds. When hi > 0, window grants are linearly reduced once buffered bytes exceed lo (default: hi/2), and clamped to 1/8 at or above hi. hi = 0 (default) disables pressure scaling entirely.",
                    "type": "object",
                    "properties": {
                        "hi": {
                            "description": "Upper pressure threshold in bytes. Grants are clamped to 1/8 at or above this level. 0 disables pressure scaling. Default: 0.",
                            "type": "integer",
                            "minimum": 0,
                            "default": 0
                        },
                        "lo": {
                            "description": "Lower pressure threshold in bytes; linear decay begins here. 0 defaults to hi/2. Default: 0.",
                            "type": "integer",
                            "minimum": 0,
                            "default": 0
                        }
                    },
                    "additionalProperties": false
                }
            },
            "additionalProperties": false
        },
        "tcp": {
            "description": "Socket options for local (application-side) TCP connections.",
            "type": "object",
            "properties": {
                "reuseport": {
                    "description": "Enable SO_REUSEPORT on local sockets. Default: false.",
                    "type": "boolean",
                    "default": false
                },
                "keepalive": {
                    "description": "Enable TCP keepalive on local sockets. Default: true.",
                    "type": "boolean",
                    "default": true
                },
                "nodelay": {
                    "description": "Enable TCP_NODELAY on local sockets. Default: true.",
                    "type": "boolean",
                    "default": true
                },
                "sndbuf": {
                    "description": "SO_SNDBUF hint in bytes for local sockets. 0 uses the system default.",
                    "type": "integer",
                    "minimum": 0
                },
                "rcvbuf": {
                    "description": "SO_RCVBUF hint in bytes for local sockets. 0 uses the system default.",
                    "type": "integer",
                    "minimum": 0
                },
                "notsent_lowat": {
                    "description": "TCP_NOTSENT_LOWAT threshold in bytes for local sockets. 0 leaves the OS default unchanged. Default: 0.",
                    "type": "integer",
                    "minimum": 0,
                    "default": 0
                },
                "backlog": {
                    "description": "Listen backlog for the local listener. Clamped to [1, 4096]. Default: 16.",
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 4096,
                    "default": 16
                }
            },
            "additionalProperties": false
        },
        "identity": {
            "description": "Identity claim and peer routing configuration.",
            "type": "object",
            "properties": {
                "claim": {
                    "description": "This node's identity string, sent to peers in the mux hello. Required when 'mux_connect' or 'listen' are configured.",
                    "type": "string"
                },
                "mux_connect": {
                    "description": "List of mux endpoint addresses to dial. One outbound session is created per entry; the peer's identity is learned from the ServerHello.",
                    "type": "array",
                    "items": {
                        "description": "Mux endpoint address to dial.",
                        "type": "string"
                    }
                },
                "listen": {
                    "description": "Local TCP listen addresses keyed by peer identity. Inbound streams are forwarded to the root 'connect' target.",
                    "type": "object",
                    "additionalProperties": {
                        "description": "Local TCP address to accept application traffic for this peer identity.",
                        "type": "string"
                    }
                }
            },
            "additionalProperties": false
        }
    },
    "additionalProperties": false
}